import React from 'react';
import ReactDOM from 'react-dom';

document.addEventListener('DOMContentLoaded', function(){
    ReactDOM.render(
        <h1>React renderuje poprawnie</h1>,
        document.getElementById('app')
    )
})
